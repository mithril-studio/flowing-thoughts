# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Conductor workspace context (never commit — may contain screenshots/logs)
.context/

# Evaluation dataset — personal voice recordings and transcripts. Never commit.
# (Default location is the app data dir; this covers --data-dir inside the repo.)
eval-data/

# Signing material and local secrets must never enter source control.
.env
.env.*
!.env.example
*.p12
*.p8
*.pem
*.key
*.password
__pycache__/
